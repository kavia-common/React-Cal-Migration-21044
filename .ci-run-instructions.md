CI runner notes:
- Run all Node commands from this directory (React-Cal-Migration-21044), not the repository root.
- Example:
  - npm install
  - npm run build
  - npm test -- --run
- The repository root does not contain a package.json; invoking npm there will fail with ENOENT.
