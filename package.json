{
  "name": "react-cal-migration-21044",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -noEmit && vite build",
    "preview": "vite preview",
    "pretest": "node -e \"try{const a=JSON.parse(process.env.npm_config_argv||'{}');if(a&&a.original){console.log('(pretest) original args:',a.original.join(' '));}}catch(e){console.log('(pretest) args parse skipped');}\"",
    "test": "vitest run --reporter=basic --",
    "test:watch": "vitest",
    "test:ci": "vitest run --reporter=basic --",
    "ci-test": "vitest run --reporter=basic --",
    "posttest": "echo \"(posttest) ignoring extra args: $npm_config_argv\"",
    "lint": "echo \"lint step not configured\""
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.0",
    "@types/node": "^24.9.1",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.7.0",
    "@vitest/coverage-v8": "^2.1.9",
    "jsdom": "^24.0.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.8",
    "vitest": "^2.1.3"
  }
}
